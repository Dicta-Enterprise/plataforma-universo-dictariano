import { FormatoBeneficiosPipe } from './formato-beneficios.pipe';

describe('FormatoBeneficiosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoBeneficiosPipe();
    expect(pipe).toBeTruthy();
  });
});
